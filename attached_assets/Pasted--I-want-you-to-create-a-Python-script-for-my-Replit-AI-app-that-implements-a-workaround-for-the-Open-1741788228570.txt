"I want you to create a Python script for my Replit AI app that implements a workaround for the OpenAI API's 413 error (Payload Too Large), which occurs when the input text exceeds the API's size limit. To solve this, design a text chunking mechanism that:

Takes a large block of text input from the user.
Splits the text into smaller chunks, ensuring each chunk is within the API's payload limit (assume a safe character limit of 4000 characters per chunk, including spaces and punctuation, unless otherwise specified).
Sends each chunk to the OpenAI API in separate calls.
Collects the responses from each API call.
Analyzes and combines the responses into a single, coherent output that makes sense to the user, preserving the context and intent of the original input.
Here’s how I want you to structure the solution:

Input Handling: Accept a single string of text from the user (e.g., via input() or a function parameter).
Chunking Logic: Split the text into chunks based on the character limit. Ensure chunks are split at logical boundaries (e.g., end of sentences or paragraphs) when possible to maintain readability and context. If a sentence exceeds the limit, split it at a word boundary.
API Interaction: Use the OpenAI Python client (openai library) to send each chunk to the API. Assume the API call is something like openai.ChatCompletion.create() (or update it to the latest equivalent). Include a placeholder for the API key and model (e.g., 'gpt-3.5-turbo').
Response Combining: After receiving responses for all chunks, combine them intelligently. For example, if the task is text summarization, ensure the final output is a cohesive summary; if it’s a question-answering task, blend the answers logically.
Error Handling: Add basic error handling for API failures (e.g., rate limits, connection issues) and retry logic if a call fails.
Output: Return the final combined response as a single string.
Additional Instructions:

Use clear variable names and add comments to explain each step of the code.
Include a sample user input and expected output in the comments for testing (e.g., a 10,000-character text split into chunks).
If the OpenAI API call requires a specific prompt for each chunk (e.g., 'Summarize this text' or 'Answer based on this'), make it configurable via a parameter.
Optimize for readability and maintainability, avoiding overly complex logic.
If you need to make assumptions (e.g., about the API endpoint or model), state them in the comments.
Do not hardcode an API key; instead, use an environment variable (e.g., os.getenv('OPENAI_API_KEY')) and note how to set it in Replit.
Please provide the complete Python code with the above functionality, and test it with a simple example (e.g., splitting a long paragraph into chunks and combining the responses). If there are any limitations or edge cases (e.g., very short inputs, API token limits), mention them in the comments.