# Skip Chromium download for puppeteer-core (we use external browser service)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_SKIP_DOWNLOAD=true

# Note: optional dependencies (canvas, geoip-lite, etc.) are skipped during deployment
# but installed locally for development
